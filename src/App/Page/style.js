export const style = theme => ({
  root: {
    minHeight: '100vh',
    display: 'flex',
    justifyContent: 'center',
    padding: '40px 0px',
    backgroundColor: theme.palette.background,
  },
})
